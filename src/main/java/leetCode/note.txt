所谓“回文”是指顺读和倒读都一样的字符串。如“XYZYX”和“xyzzyx”都是回文。  回文中最多一个字符出现奇数次 其余的都出现偶数次。

常用函数：
Arrays.sort();
String toCharArray()
String charAt()

// 四则运算 逆波兰表达式   有效的括号
Stack<>
    push() pop throws EmptyException() size() isEmpty()

Math.random()  随机生成一个 0~1的double数据。

20 - 50*Math.random() 生成一个 15 ~20 之间的随机数


>> 1 右移一位 相当于除以2且向下取整

